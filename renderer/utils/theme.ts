import { createTheme } from "@mui/material";

export default createTheme({
  palette: {
    text: {
      primary: "white",
      secondary: "lightgrey",
      disabled: "grey"
    },
    mode: "dark"
  }
});
