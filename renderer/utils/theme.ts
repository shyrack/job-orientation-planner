import { createTheme } from "@mui/material";

export default createTheme({
  palette: {
    text: {
      primary: "#fff",
      secondary: "#999",
      disabled: "#777"
    },
    mode: "dark"
  },
  shape: {
    borderRadius: 20
  }
});
