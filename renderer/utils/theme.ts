import { createTheme } from "@mui/material";

export default createTheme({
  palette: {
    text: {
      primary: "#fff",
      secondary: "#999",
      disabled: "#777"
    },
    secondary: {
      light: "#8958c6",
      main: "#743eb6",
      dark: "#613498"
    },
    mode: "dark"
  },
  shape: {
    borderRadius: 20
  }
});
