import { Typography } from "@mui/material";
import PageProvider from "../components/provider/PageProvider";

export default function CreditsPage() {
  return (
    <PageProvider>
      <Typography variant="h4">CREDITS COME HERE</Typography>
    </PageProvider>
  );
}
